/**
 * Tijdelijke centrale schakelaar voor de winkel-app.
 *
 * "Op rekening" (tab) staat nu UIT — er wordt alleen met Mollie betaald
 * (testfase). Zodra de admin-instelling (Instellingen → Betaalmethodes)
 * gebouwd is, wordt deze waarde uit de database gelezen i.p.v. hier.
 */
export const OP_REKENING_ENABLED = false;
