{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["fra1"],
  "buildCommand": "NODE_ENV=development payload run scripts/init-db.ts && next build",
  "functions": {
    "src/app/(payload)/admin/[[...segments]]/page.tsx": {
      "maxDuration": 60
    },
    "src/app/(payload)/api/[...slug]/route.ts": {
      "maxDuration": 60
    }
  }
}
